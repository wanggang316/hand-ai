[package]
name = "hand-agent"
version = "0.1.0"
edition = "2024"

[dependencies]
model = { path = "../model" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
futures = "0.3"
async-stream = "0.3"
thiserror = "2"
tracing = "0.1"
