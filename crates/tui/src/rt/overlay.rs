//! Overlays and modals (skeleton).
//!
//! Will layer transient views (autocomplete popups, dialogs, toasts) on top
//! of the base view inside the viewport, with a stack that routes input to
//! the topmost overlay first.
