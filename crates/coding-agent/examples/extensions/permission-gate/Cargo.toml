[package]
name = "ext-permission-gate"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
hand-coding-agent = { path = "../../.." }
async-trait = "0.1"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
serde_json = "1"
